# Agent Instructions

MANDATORY: Before making ANY code changes or writing ANY code, read `.roomodes` in the repository root and follow all rules in it. `.roomodes` is the canonical source of project rules for every AI agent/model (Kilo, Claude Code, Cursor, Copilot, Codex, etc.), not just Kilo. Do not rely on memory or previously seen rules — re-read `.roomodes` at the start of each session/task before editing files.
