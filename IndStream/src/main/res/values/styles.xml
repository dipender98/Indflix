<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Flat dialog theme: no floating shadow/border gradient around the edges.
        Used by Settings.kt so the dialog background is a single solid colour.
    -->
    <style name="IndStream.FlatDialog" parent="Theme.AppCompat.DayNight.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.6</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowFrame">@null</item>
    </style>
</resources>
