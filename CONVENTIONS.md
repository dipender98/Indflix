MANDATORY: Before making ANY code changes or writing ANY code, read `.roomodes` in the repository root and follow all rules in it. `.roomodes` is the canonical source of project rules for every AI agent and must be re-read at the start of every task before editing files. Aider users: add `read: CONVENTIONS.md` to `.aider.conf.yml` so this file is auto-loaded.
