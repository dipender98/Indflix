# Nothing to keep here; the plugin entrypoint is discovered via the
# @CloudstreamPlugin annotation + META-INF at runtime, and R8 does not run
# in debug. Release keeps are handled by the shared root proguard config.
